import React from 'react';
import ReactDOM from 'react-dom';
import {PrototypeFS} from '../containers/staticFoodStore';

const ChooseStore = (props) => (

    <div>
        <PrototypeFS/>
    </div>
);

export default ChooseStore;
