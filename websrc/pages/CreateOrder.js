import React from 'react';
import ReactDOM from 'react-dom';
import NewOrderForm from '../containers/NewOrderFormContainer';

const CreateOrder = (props) => (

    <div>
        <NewOrderForm/>
    </div>
);

export default CreateOrder;
